name: ${project.name}
version: ${project.version}+${ciSystem}-b${buildNumber}.git-${commit}
description: ${project.description}
website: ${project.url}
author: ${project.organization.name}

main: ${mainClass}
load: startup
platform: ALL

data:
  protocol: ${protocolVersion}

protocols:
  - ${protocolClass}
