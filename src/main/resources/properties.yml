name: Vanilla
version: ${releaseVersion} b${buildNumber}
description: Vanilla is a plugin for the Spout platform that provides vanilla Minecraft functionality.
author: SpoutDev
website: http://www.spout.org

main: org.spout.vanilla.VanillaPlugin
load: startup
platform: BOTH

data:
    protocol: ${protocolVersion}
